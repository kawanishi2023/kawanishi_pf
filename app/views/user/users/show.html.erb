<h1>user detail</h1>

<table>
  <tr>
    <th>name :</th>
    <td><%= current_user.name %></td>
  </tr>
  <tr>
    <th>email:</th>
    <td><%= current_user.email %></td>
  </tr>
</table>

<%= link_to "Edit", edit_user_path(current_user)  %>

<%= link_to user_withdrawal_path, method: :patch, "data-confirm" => "本当に削除しますか？", class:'btn btn-outline-danger mb-3' do %>
 <i class="fas fa-heart-broken"></i>退会する
<% end %>