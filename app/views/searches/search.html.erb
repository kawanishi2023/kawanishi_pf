<h2>Results index</h2>

<table class="table table-hover table-inverse">
    <tbody>
      <% @songs.each do |song| %>
        <tr>
          <td><%= song.name %></td>
          <td><%= song.artist %></td>
        </tr>
      <% end %>
    </tbody>
</table>